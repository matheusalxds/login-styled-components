import { addPixel } from '../tools';

const space = 8;

const space0 = addPixel(space / 2);
const space1 = addPixel(space);
const space2 = addPixel(space * 2);
const space3 = addPixel(space * 3);
const space5 = addPixel(space * 5);

export { space0, space1, space2, space3, space5 };
